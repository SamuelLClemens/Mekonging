// Allergy & dietary phrases, per language, surfaced inside the phrasebook food
// section. SAFETY-CRITICAL: these are an AI/native pass — have a local confirm the
// wording before relying on them for a serious allergy. Shape per language code:
//   { en, script, roman }
// Filled by the content workflow (assembled from per-language JSON).
export const ALLERGENS = {};
